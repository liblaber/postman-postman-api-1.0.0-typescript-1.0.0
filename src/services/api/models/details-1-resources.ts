// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const details1Resources = z.lazy(() => {
  return z.object({
    id: z.string().optional(),
    url: z.string().optional(),
  });
});

/**
 *
 * @typedef  {Details1Resources} details1Resources
 * @property {string} - The ID of the assigned resource.
 * @property {string} - The task's assigned resource URL.
 */
export type Details1Resources = z.infer<typeof details1Resources>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const details1ResourcesResponse = z.lazy(() => {
  return z
    .object({
      id: z.string().optional(),
      url: z.string().optional(),
    })
    .transform((data) => ({
      id: data['id'],
      url: data['url'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const details1ResourcesRequest = z.lazy(() => {
  return z.object({ id: z.string().nullish(), url: z.string().nullish() }).transform((data) => ({
    id: data['id'],
    url: data['url'],
  }));
});
