// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const rolesUser: any = z.lazy(() => {
  return z.object({
    id: z.number().optional(),
    description: z.string().optional(),
    displayName: z.string().optional(),
  });
});

/**
 * Information about the user role.
 * @typedef  {RolesUser} rolesUser - Information about the user role. - Information about the user role.
 * @property {number} - The role's ID.
 * @property {string} - The role's description.
 * @property {string} - The role's display name.
 */
export type RolesUser = z.infer<typeof rolesUser>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const rolesUserResponse: any = z.lazy(() => {
  return z
    .object({
      id: z.number().optional(),
      description: z.string().optional(),
      displayName: z.string().optional(),
    })
    .transform((data) => ({
      id: data['id'],
      description: data['description'],
      displayName: data['displayName'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const rolesUserRequest: any = z.lazy(() => {
  return z
    .object({ id: z.number().nullish(), description: z.string().nullish(), displayName: z.string().nullish() })
    .transform((data) => ({
      id: data['id'],
      description: data['description'],
      displayName: data['displayName'],
    }));
});
