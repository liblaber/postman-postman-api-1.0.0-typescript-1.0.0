// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const mergeEnvironmentForkEnvironment: any = z.lazy(() => {
  return z.object({
    uid: z.string().optional(),
  });
});

/**
 * Information about the merged environment.
 * @typedef  {MergeEnvironmentForkEnvironment} mergeEnvironmentForkEnvironment - Information about the merged environment. - Information about the merged environment.
 * @property {string} - The merged environment's ID.
 */
export type MergeEnvironmentForkEnvironment = z.infer<typeof mergeEnvironmentForkEnvironment>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const mergeEnvironmentForkEnvironmentResponse: any = z.lazy(() => {
  return z
    .object({
      uid: z.string().optional(),
    })
    .transform((data) => ({
      uid: data['uid'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const mergeEnvironmentForkEnvironmentRequest: any = z.lazy(() => {
  return z.object({ uid: z.string().nullish() }).transform((data) => ({
    uid: data['uid'],
  }));
});
