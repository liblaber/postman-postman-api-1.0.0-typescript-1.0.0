// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const getEnvironmentForksMeta: any = z.lazy(() => {
  return z.object({
    total: z.number().optional(),
    nextCursor: z.string().optional().nullable(),
  });
});

/**
 * The response's meta information for paginated results.
 * @typedef  {GetEnvironmentForksMeta} getEnvironmentForksMeta - The response's meta information for paginated results. - The response's meta information for paginated results.
 * @property {number} - The total number of forked environments.
 * @property {string} - The pagination cursor that points to the next record in the results set.
 */
export type GetEnvironmentForksMeta = z.infer<typeof getEnvironmentForksMeta>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const getEnvironmentForksMetaResponse: any = z.lazy(() => {
  return z
    .object({
      total: z.number().optional(),
      nextCursor: z.string().optional().nullable(),
    })
    .transform((data) => ({
      total: data['total'],
      nextCursor: data['nextCursor'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const getEnvironmentForksMetaRequest: any = z.lazy(() => {
  return z.object({ total: z.number().nullish(), nextCursor: z.string().nullish() }).transform((data) => ({
    total: data['total'],
    nextCursor: data['nextCursor'],
  }));
});
