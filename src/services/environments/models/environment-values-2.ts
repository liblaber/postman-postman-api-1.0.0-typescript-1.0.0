// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const environmentValues2: any = z.lazy(() => {
  return z.object({
    enabled: z.boolean().optional(),
    key: z.string().optional(),
    value: z.string().optional(),
    type: z.string().optional(),
  });
});

/**
 *
 * @typedef  {EnvironmentValues2} environmentValues2
 * @property {boolean} - If true, the variable is enabled.
 * @property {string} - The variable's name.
 * @property {string} - The variable's value.
 * @property {ValuesType2} - The variable type.
 */
export type EnvironmentValues2 = z.infer<typeof environmentValues2>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const environmentValues2Response: any = z.lazy(() => {
  return z
    .object({
      enabled: z.boolean().optional(),
      key: z.string().optional(),
      value: z.string().optional(),
      type: z.string().optional(),
    })
    .transform((data) => ({
      enabled: data['enabled'],
      key: data['key'],
      value: data['value'],
      type: data['type'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const environmentValues2Request: any = z.lazy(() => {
  return z
    .object({
      enabled: z.boolean().nullish(),
      key: z.string().nullish(),
      value: z.string().nullish(),
      type: z.string().nullish(),
    })
    .transform((data) => ({
      enabled: data['enabled'],
      key: data['key'],
      value: data['value'],
      type: data['type'],
    }));
});
