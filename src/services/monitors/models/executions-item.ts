// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const executionsItem = z.lazy(() => {
  return z.object({
    name: z.string().optional(),
  });
});

/**
 * Information about the executed item.
 * @typedef  {ExecutionsItem} executionsItem - Information about the executed item. - Information about the executed item.
 * @property {string} - The executed item's name.
 */
export type ExecutionsItem = z.infer<typeof executionsItem>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const executionsItemResponse = z.lazy(() => {
  return z
    .object({
      name: z.string().optional(),
    })
    .transform((data) => ({
      name: data['name'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const executionsItemRequest = z.lazy(() => {
  return z.object({ name: z.string().nullish() }).transform((data) => ({
    name: data['name'],
  }));
});
