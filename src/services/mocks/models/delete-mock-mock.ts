// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const deleteMockMock = z.object({
  id: z.string().optional(),
  uid: z.string().optional(),
});

/**
 * Information about the mock server.
 * @typedef  {DeleteMockMock} deleteMockMock - Information about the mock server. - Information about the mock server.
 * @property {string} - The mock server's ID.
 * @property {string} - The mock server's unique ID.
 */
export type DeleteMockMock = z.infer<typeof deleteMockMock>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const deleteMockMockResponse = z
  .object({
    id: z.string().optional(),
    uid: z.string().optional(),
  })
  .transform((data) => ({
    id: data['id'],
    uid: data['uid'],
  }));

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const deleteMockMockRequest = z
  .object({ id: z.string().nullish(), uid: z.string().nullish() })
  .transform((data) => ({
    id: data['id'],
    uid: data['uid'],
  }));
