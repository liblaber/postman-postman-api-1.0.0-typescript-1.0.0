// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { getMockMock, getMockMockRequest, getMockMockResponse } from './get-mock-mock';

/**
 * The shape of the model inside the application code - what the users use
 */
export const getMock = z.lazy(() => {
  return z.object({
    mock: getMockMock.optional(),
  });
});

/**
 *
 * @typedef  {GetMock} getMock
 * @property {GetMockMock}
 */
export type GetMock = z.infer<typeof getMock>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const getMockResponse = z.lazy(() => {
  return z
    .object({
      mock: getMockMockResponse.optional(),
    })
    .transform((data) => ({
      mock: data['mock'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const getMockRequest = z.lazy(() => {
  return z.object({ mock: getMockMockRequest.nullish() }).transform((data) => ({
    mock: data['mock'],
  }));
});
