// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const publishMockMock = z.lazy(() => {
  return z.object({
    id: z.string().optional(),
  });
});

/**
 *
 * @typedef  {PublishMockMock} publishMockMock
 * @property {string} - The mock server's ID.
 */
export type PublishMockMock = z.infer<typeof publishMockMock>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const publishMockMockResponse = z.lazy(() => {
  return z
    .object({
      id: z.string().optional(),
    })
    .transform((data) => ({
      id: data['id'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const publishMockMockRequest = z.lazy(() => {
  return z.object({ id: z.string().nullish() }).transform((data) => ({
    id: data['id'],
  }));
});
