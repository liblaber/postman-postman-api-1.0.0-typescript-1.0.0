// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const getCollectionRolesUser: any = z.lazy(() => {
  return z.object({
    role: z.string().optional(),
    id: z.number().optional(),
  });
});

/**
 * Information about the user role.
 * @typedef  {GetCollectionRolesUser} getCollectionRolesUser - Information about the user role. - Information about the user role.
 * @property {UserRole} - The role type:
- `VIEWER` — Can view, fork, and export collections.
- `EDITOR` — Can edit collections directly.

 * @property {number} - The role's ID.
 */
export type GetCollectionRolesUser = z.infer<typeof getCollectionRolesUser>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const getCollectionRolesUserResponse: any = z.lazy(() => {
  return z
    .object({
      role: z.string().optional(),
      id: z.number().optional(),
    })
    .transform((data) => ({
      role: data['role'],
      id: data['id'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const getCollectionRolesUserRequest: any = z.lazy(() => {
  return z.object({ role: z.string().nullish(), id: z.number().nullish() }).transform((data) => ({
    role: data['role'],
    id: data['id'],
  }));
});
