// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const panCreateApiApi = z.object({
  id: z.string(),
  parentFolderId: z.number(),
});

/**
 *
 * @typedef  {PanCreateApiApi} panCreateApiApi
 * @property {string} - The API's ID.
 * @property {number} - The API's parent folder ID.
 */
export type PanCreateApiApi = z.infer<typeof panCreateApiApi>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const panCreateApiApiResponse = z
  .object({
    id: z.string(),
    parentFolderId: z.number(),
  })
  .transform((data) => ({
    id: data['id'],
    parentFolderId: data['parentFolderId'],
  }));

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const panCreateApiApiRequest = z
  .object({ id: z.string().nullish(), parentFolderId: z.number().nullish() })
  .transform((data) => ({
    id: data['id'],
    parentFolderId: data['parentFolderId'],
  }));
