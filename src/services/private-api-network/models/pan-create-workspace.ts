// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import {
  panCreateWorkspaceWorkspace,
  panCreateWorkspaceWorkspaceRequest,
  panCreateWorkspaceWorkspaceResponse,
} from './pan-create-workspace-workspace';

/**
 * The shape of the model inside the application code - what the users use
 */
export const panCreateWorkspace: any = z.lazy(() => {
  return z.object({
    workspace: panCreateWorkspaceWorkspace.optional(),
  });
});

/**
 *
 * @typedef  {PanCreateWorkspace} panCreateWorkspace
 * @property {PanCreateWorkspaceWorkspace}
 */
export type PanCreateWorkspace = z.infer<typeof panCreateWorkspace>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const panCreateWorkspaceResponse: any = z.lazy(() => {
  return z
    .object({
      workspace: panCreateWorkspaceWorkspaceResponse.optional(),
    })
    .transform((data) => ({
      workspace: data['workspace'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const panCreateWorkspaceRequest: any = z.lazy(() => {
  return z.object({ workspace: panCreateWorkspaceWorkspaceRequest.nullish() }).transform((data) => ({
    workspace: data['workspace'],
  }));
});
